import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 50,
    width: 346,
    backgroundColor: "#FFF",
    borderRadius: 10,
    textAlign: "center",
    fontSize: 20,
    margin: 15,
  },
});
